import React from 'react';
import { FadeLoader } from 'react-spinners';

const loadingAnimationColor = 'blue';
const LoadingSpinner = () => <FadeLoader color={loadingAnimationColor} />;

export default LoadingSpinner;
